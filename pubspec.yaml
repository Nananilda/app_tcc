name: app_tcc
description: "Sistema de monitoramento industrial"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.12.2 <4.0.0' # Ajustado para o formato padrão do SDK do Dart

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^17.4.0
  provider: ^6.1.2
  fl_chart: ^1.2.0
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
